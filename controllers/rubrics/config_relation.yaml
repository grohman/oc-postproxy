# ===================================
#  Relation Behavior Config
# ===================================

recipients:
    label: ' '
    emptyMessage: Нет получателей
    view:
      toolbarButtons: create|add|remove
      showSorting: true,
      showCheckboxes: true,
      recordsPerPage: 10,
      list: $/idesigning/postproxy/models/recipient/pivot/columns.yaml
    manage:
      list: $/idesigning/postproxy/models/recipient/columns.yaml
      form: $/idesigning/postproxy/models/recipient/fields.yaml