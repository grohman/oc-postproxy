# ===================================
#  Form Field Definitions
# ===================================

fields:
    name:
        label: Название
    service_id:
      label: Сервис
      type: dropdown
    options:
      type: servicesWidget
      dataAttribute: options
      dependsOn: service_id
    recipients:
      label: Получатели
      type: partial
      context: [update]